sudo: required # Added only to join beta (https://blog.travis-ci.com/2015-10-14-opening-up-ubuntu-trusty-beta/)
dist: trusty

language: java

jdk:
  - oraclejdk8
  - oraclejdk7
  - openjdk8
  # OpenJDK 7 is currently broken
  #   https://github.com/travis-ci/travis-ci/issues/5227
  #   https://github.com/travis-ci/travis-ci/issues/5669
  # Since we don't exspect different behavior from other JDKs, just skip the test.
  # - openjdk7

notifications:
  email: false
